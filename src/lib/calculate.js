function calculate(aplicacao, capitalizacao, dias, juros) {
    let resultado;
    switch (capitalizacao) {
        case 'continua':
        break;
        case 'composta':
        break;
        default:
            resultado = 0;
    }
    return resultado;
}

export default calculate;